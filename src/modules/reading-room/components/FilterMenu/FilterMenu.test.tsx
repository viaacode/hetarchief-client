// eslint-disable-next-line @typescript-eslint/no-unused-vars
import FilterMenu from './FilterMenu';

describe('Components', () => {
	describe('<FilterMenu />', () => {
		it('Should render', () => {
			// test stuff
		});
	});
});
