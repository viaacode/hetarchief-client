import { FC } from 'react';

import styles from './Navigation.module.scss';

const Navigation: FC = () => {
	return <nav className={styles['c-navigation']}>This is the nav</nav>;
};

export default Navigation;
