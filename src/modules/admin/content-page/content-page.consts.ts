const CONTENT = 'content';
const ID = ':contentPageId';

export const CONTENT_PAGE_PATHS = {
	overview: `/${CONTENT}`,
	detail: `/${CONTENT}/${ID}`,
	create: `/${CONTENT}/${ID}/maak`,
	edit: `/${CONTENT}/${ID}/bewerk`,
};
